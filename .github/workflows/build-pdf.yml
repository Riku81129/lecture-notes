name: Build LaTeX PDFs

on:
  push:
    paths:
      - "tex/**"
      - ".github/workflows/build-pdf.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Compile LaTeX
        uses: xu-cheng/latex-action@v3
        with:
          working_directory: tex/Chomsky1956
          root_file: 
            Chomsky1956_intro.tex
            Chomsky1956_2.4.tex
          args: -pdfdvi -latexoption=-interaction=nonstopmode -latexoption=-halt-on-error -latexoption=-file-line-error

      - name: Move PDF
        run: |
          mkdir -p pdfs
          cp tex/Chomsky1956/Chomsky1956_intro.pdf pdfs/Chomsky.pdf

      - name: Commit PDF
        uses: EndBug/add-and-commit@v9
        with:
          add: "pdfs/Chomsky1956.pdf"
          message: "Update compiled PDF"
